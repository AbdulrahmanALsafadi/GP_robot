/*
 * Created by ArduinoGetStarted.com
 *
 * This example code is in the public domain
 *
 * Tutorial page: https://arduinogetstarted.com/tutorials/arduino-force-sensor
 */

#define FORCE_SENSOR_PIN_0 A0
#define FORCE_SENSOR_PIN_1 A1
#define FORCE_SENSOR_PIN_2 A2
#define FORCE_SENSOR_PIN_3 A3
#define FORCE_SENSOR_PIN_4 A4
#define FORCE_SENSOR_PIN_5 A5

void setup() {
  Serial.begin(9600);
}

void loop() {
  int Reading0 = analogRead(FORCE_SENSOR_PIN_0);
  int Reading1 = analogRead(FORCE_SENSOR_PIN_1);
  int Reading2 = analogRead(FORCE_SENSOR_PIN_2);
  int Reading3 = analogRead(FORCE_SENSOR_PIN_3);
  int Reading4 = analogRead(FORCE_SENSOR_PIN_4);
  int Reading5 = analogRead(FORCE_SENSOR_PIN_5);


  Serial.print("Force sensor reading A0   = ");
  Serial.println(Reading0);
  
  Serial.print("Force sensor reading A1   = ");
  Serial.println(Reading1); // print the raw analog reading

   Serial.print("Force sensor reading A2   = ");
  Serial.println(Reading2);

  Serial.print("Force sensor reading A3   = ");
  Serial.println(Reading3);

  Serial.print("Force sensor reading A4   = ");
  Serial.println(Reading4);
  Serial.print("Force sensor reading A5   = ");
  Serial.println(Reading5);
  delay(1000);
}
